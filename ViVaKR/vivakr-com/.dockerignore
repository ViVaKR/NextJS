# .dockerignore 예시
# Git 관련 파일 제외
.git
.gitignore

# node_modules 제외 (빌드 컨테이너에서 설치하므로)
node_modules

# 빌드 결과물 제외 (컨테이너 내부에서 생성)
.next
build
out

# 기타 불필요 파일
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.tsbuildinfo

# .env 파일들은 빌드 시 필요 없을 수 있음
# 만약 pnpm build 가 .env.local 값을 참조해야 한다면 아래 라인 주석 처리 또는 삭제
# .env*
# .env
# .env.development.local
# .env.test.local
# .env.production.local
# .env.local

# vercel 관련 파일
.vercel

# certificates 폴더 (만약 빌드에 필요 없다면)
certificates
